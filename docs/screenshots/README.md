# Screenshot checklist

The main README embeds each of these inline, right under the feature it shows (no standalone "Screenshots" section) — drop each file into this folder (`docs/screenshots/`) with the exact filename below and it renders automatically on GitHub, no other changes needed.

PNG or JPG both work; keep the extension consistent with what's listed (rename on your end if you'd rather use `.jpg`, then update the one line in `../../README.md` that references it).

| Filename | What to capture | Status |
|---|---|---|
| `landing.png` | The public landing page (logged out), full hero section | ✅ done |
| `auth.png` | The login/signup screen — either mode, whichever looks cleaner | ✅ done |
| `dashboard.png` | "My Resumes" dashboard after logging in, with at least one resume card visible | ✅ done |
| `builder.png` | The resume builder screen — form on the left, live preview on the right, mid-fill with real-looking sample content (not empty fields) | ✅ done |
| `analyze.png` | The "Analyze against a job" results screen — score ring + matched/missing keywords + suggestions all visible | ✅ done |
| `ats-score.png` | The independent ATS Score results screen | ✅ done |
| `admin.png` | The admin panel — stats dashboard + user table both visible if it fits, otherwise the stats panel | ✅ done |
| `bg-removal.png` | The Contact section's background-removal panel, ideally mid-flow showing the color swatch picker | still open |

Dropped `templates.png` from this list — not referenced in the main README anymore.

Nice to have, not required — the README works fine without these two:

| Filename | What to capture |
|---|---|
| `admin-detail.png` | The per-user detail modal opened from the admin panel |
| `usage.png` | The self-service Usage screen (quota bar, recent activity) |

Use real-looking sample data where you can (a filled-out name/experience, not "asdf asdf") — screenshots with placeholder junk text read as unfinished even when the feature works fine.
